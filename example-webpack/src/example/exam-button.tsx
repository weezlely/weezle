import React from "react";
import { Button } from "taeo-ui";

const ExamButton = () => {
  return (
    <Button variant="primary" background="red" color="#fff">
      버튼
    </Button>
  );
};

export default ExamButton;
